// primitive type
// string
// number
// boolean
// undefined
// null

// explicit type declaration
const user_name: string = "my name is faruk";
let number: number = 1234;
let isAdmin: boolean = true;

// explicit type declaration
const i_user_name = "my name is faruk";
let i_number = 1234;
let i_isAdmin = true;

// when we declare varibale but don't asigin any value it's type will be any
let data;
// here data is any type variable

// when we declare variable using let and assign undefined to this variable. then the type will be any. on the other hand if the variable declare as const then it's type willbe undefinde

// any type
let value = undefined;

// undefine type
const value_2 = undefined;
